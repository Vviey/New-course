/**
 * This is an empty file that replaces the original Vite config helper script.
 * It's maintained for compatibility but contains no Replit-specific code.
 */
console.log('Local development mode - running without Replit configuration');