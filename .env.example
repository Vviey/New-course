# Environment configuration for local development

# Vite environment variables
VITE_ALLOWED_HOSTS=all
VITE_HMR_HOST=localhost
VITE_SERVER_HOST=0.0.0.0

# Application configuration
VITE_APP_ENV=development
VITE_APP_DEFAULT_REALM=1

# Local development flag to skip Replit-specific code
LOCAL_DEV=true
SKIP_REPLIT=true

# Database configuration - not required for local development
# Uncomment and set these only if you want to connect to a real database
# DATABASE_URL=postgres://username:password@localhost:5432/database-name
# PGUSER=username
# PGHOST=localhost
# PGPASSWORD=password
# PGDATABASE=database-name
# PGPORT=5432

# Session secret - set this to a random string for development
SESSION_SECRET=local-development-secret